<!DOCTYPE phpunit>
<phpunit bootstrap="./bootstrap.php" colors="true">
    <testsuite name="All Tests">
        <directory>.</directory>
    </testsuite>
    <filter>
        <whitelist>
            <directory suffix=".php">./</directory>
        </whitelist>
    </filter>
</phpunit>